import React from 'react';

export default function Page() {
  return (
    <main style={{padding: 24, fontFamily: 'Inter, system-ui, sans-serif'}}>
      <h1>HabitFlow — Frontend</h1>
      <p>Bem-vindo! Este é um scaffold inicial. Use <code>npx create-next-app@latest frontend --typescript --app</code> para gerar a aplicação completa.</p>
    </main>
  );
}
